# Notch notification test

This pull request is a harmless live fixture for verifying that GitPings:

- discovers a newly authored pull request in a selected repository;
- reports its CI and mergeability state;
- surfaces the new-PR event around the MacBook notch; and
- opens the pull request in the default browser when clicked.

The file can be removed after the end-to-end notification test is complete.
